import React, { useState, useEffect, useRef } from "react";
import { useHistory } from 'react-router-dom';
import { SyncOutlined, EditOutlined, UserAddOutlined, CloseOutlined } from '@ant-design/icons';
import 'antd/dist/reset.css';
import { Select, Button, Space, Table, Input, Modal, Popconfirm, notification, Tag, Switch } from 'antd';
import "./table.css"
const { TextArea } = Input;

const Main = () => {


    return (
        <div>
            HELLO STEP
        </div >

    )
}
export default Main